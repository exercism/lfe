# Instructions Append

## Implementation

Represent an empty tree as the atom `empty-tree`.

Represent a node with the tuple `#(tree-node Data Left Right)`, with the `tree-node` atom, the node's data, the left subtree, and the right subtree.
